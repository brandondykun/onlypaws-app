# CLAUDE.md

Guidance for Claude Code when working in the `only-paws-app` mobile app.

The shared coding rules below are the single source of truth (`.ai/rules/main.mdc`)
and are also applied in Cursor via the `.cursor/rules` symlink. Edit that file, not
this one, to change the rules.

@.ai/rules/main.mdc
